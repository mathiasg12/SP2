let token = localStorage.getItem('Token');
let header = document.querySelector('header');
/**
 * trying to make a function that change the header depending if the user is logged in
 */
window.addEventListener('load', () => {
  if (token == undefined) {
  }
});
